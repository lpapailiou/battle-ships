<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorOnSurface">@color/colorPrimaryDark</item>
        <item name="android:textColor">@color/colorAccent</item>

        <!-- background image: sonar grid -->
        <item name="android:windowBackground">@drawable/sonar_grid</item>

        <!-- button styles -->
        <item name="android:buttonStyle">@style/ButtonStyle</item>
        <item name="colorOnPrimary">@color/colorAccent</item>  <!-- button text color -->

        <!-- toolbar styles -->
        <item name="theme">@style/ToolBarTextTheme</item>
        <item name="popupMenuBackground">@color/colorPrimary</item>

        <!-- error styles -->
        <item name="colorError">@color/colorComplementary</item>
        <item name="colorOnError">@color/colorComplementary</item>
    </style>

    <!-- #################### helper styles #################### -->

    <style name="ToolBarTextTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/colorAccent</item>
    </style>

    <style name="ButtonStyle" parent="android:Widget.Button">
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:textSize">12sp</item>
        <item name="android:padding">8dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:gravity">center</item>
    </style>

</resources>
